export const linkBase = ['link.id'];

export const linkDetail = [
  'link.send_id',
  'link.receive_id',
  'link.unread_count',
  'link.title',
  'link.type',
  'link.message'
];

export const linkOther = ['link.update_at', 'link.create_at'];
